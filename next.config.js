/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains : [
            'localhost',
            'winterglory.up.railway.app',
        ]
    }
}

module.exports = nextConfig